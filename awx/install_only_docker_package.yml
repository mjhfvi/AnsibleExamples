---
- name: PlayBook To Install Docker
  hosts: all
  tasks:
  
  - name: Install all the packages
    apt:
      pkg:
      - apt-transport-https
      - ca-certificates
      - python3-pip
      - software-properties-common
      - docker.io
      - python-setuptools
